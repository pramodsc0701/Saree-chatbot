my_list = [2, 7, 11, 15]
target_sum = 9



select a.col1, sum() over(order by col1 row between )  from abc a
outer join abc a
on a.col1 = b.col1
where  = 9


select s.id , case when s.id <> t.id and s.name <> t.name and c1 = source
then 'New in Source'

when s.id = t.id and s.name <> t.name
then 'mismatch'

when s.id <> t.id and s.name <> t.name and c1 = target
then 'new in target'  

end as 'comments'

from source s
outer join target t
on s.id = t.id 
